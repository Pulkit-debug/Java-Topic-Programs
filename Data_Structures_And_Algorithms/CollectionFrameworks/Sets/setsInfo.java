package Data_Structures_And_Algorithms.CollectionFrameworks.Sets;

public class setsInfo {

	
	//***************************** Sets **************************
	
	/*
	 * Set Interface: The set interface of the java collection framework provides the features of mathematical set in java. It extends
	 * the collection interface. Unlinke the list interface sets cannot contain duplicate elements 
	 * 
	 * 
	 * In order to use functionalities of the set interface we can use the following classes:
	 * 
	 * 1. HashSet
	 * 2. LinkedHashSet
	 * 3. EnumSet
	 * 4. TreeSet
	 * 
	 * these above all four classes implemetns set interface
	 * 
	 * 
	 *NOTE: HashSet uses hash map internally which works on key value pair 
	 * 
	 * 
	 * ----------------------------------- Method of Set --------------------------------
	 * 
	 * 1. add(element)
	 * 2. addAll(collection)
	 * 3. remove(element)
	 * 4. removeAll()
	 * 5. retainAll()
	 * 6. clear()
	 * 7. size()
	 * 8. contains()
	 * 9. containsAll()
	 * 10. isEmpty()
	 * 11. toArray()
	 * 
	 * 
	 * ------------------------------- Mathematical operations of Set---------------
	 * 
	 * 1. UNION: to get the union of two sets x and y we can use x.addAll(y)
	 * 
	 * 2. INTERSECTION: to get the intersection of two sets x and y we can use x.retainAll(y)
	 * 
	 * 3. Subset: to check if x is a subset of y we can use y.containsAll(x)
	 * 
	 * 
	 * */
	
	
}
